# Authentication System Architecture

## High-Level System Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                         │
│                                                                   │
│  ┌────────────────────────┐         ┌──────────────────────┐   │
│  │   Login Page           │         │  Protected Pages      │   │
│  │  (/login)              │         │  (/workflows/*, etc)  │   │
│  │                        │         │                       │   │
│  │ Email/Password Input   │         │  Require Auth Check   │   │
│  │ Submit Form            │         │  Role Verification    │   │
│  └────────────────────────┘         │  Render Page Content  │   │
│           │                         └──────────────────────┘   │
│           │                                   │                 │
│           └───────────────────────────────────┘                 │
│                        │                                         │
│                   Send to Server                                 │
│                        │                                         │
└────────────────────────┼─────────────────────────────────────────┘
                         │
            HTTP Request │ loginAction() / Page Render
                         │
┌────────────────────────┼─────────────────────────────────────────┐
│                  SERVER (Next.js)                                 │
│                        │                                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          Authentication System (src/lib/auth.ts)         │   │
│  │                                                           │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  DEMO_USERS Map                                  │   │   │
│  │  │  {                                               │   │   │
│  │  │    'admin@liyali.com': {                        │   │   │
│  │  │      password: 'password123',                   │   │   │
│  │  │      user: {                                     │   │   │
│  │  │        id, name, email, role, department, ...   │   │   │
│  │  │      }                                           │   │   │
│  │  │    },                                            │   │   │
│  │  │    ... (6 more demo users)                       │   │   │
│  │  │  }                                               │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                        │                                 │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  Authentication Functions                        │   │   │
│  │  │                                                  │   │   │
│  │  │  • login(email, password)                       │   │   │
│  │  │  • logout()                                      │   │   │
│  │  │  • getSession()                                  │   │   │
│  │  │  • getCurrentUser()                              │   │   │
│  │  │  • hasRole(role)                                 │   │   │
│  │  │  • isAdmin()                                     │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                        │                                         │
│                        ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Server Actions (src/app/_actions/auth-actions)  │   │
│  │                                                           │   │
│  │  loginAction() ──► validateCredentials() ──► createSession()
│  │  logoutAction() ──► clearSession()                        │   │
│  │  getCurrentUserAction() ──► readSession()                │   │
│  │  requireAuth() ──► redirect if not authenticated          │   │
│  │  requireRole() ──► redirect if wrong role                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                        │                                         │
│                        ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            HTTP-Only Cookie Storage                       │   │
│  │                                                           │   │
│  │  Cookie Name: auth_session                              │   │
│  │  Value: Base64(JSON{                                     │   │
│  │           user: { id, name, email, role, ... },         │   │
│  │           expiresAt: timestamp                           │   │
│  │         })                                               │   │
│  │  HttpOnly: true (safe from XSS)                          │   │
│  │  Secure: true (HTTPS only in production)                 │   │
│  │  SameSite: lax (CSRF protection)                         │   │
│  │  MaxAge: 86400 seconds (24 hours)                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                        │                                         │
└────────────────────────┼─────────────────────────────────────────┘
                         │
            Response │ Set-Cookie: auth_session=...
                         │
┌────────────────────────┼─────────────────────────────────────────┐
│                  CLIENT (Browser)                                │
│                        │                                         │
│        Cookie Stored in Browser │ localStorage/sessionStorage   │
│                        │                                         │
│                        ▼                                         │
│        ┌──────────────────────────────────────┐               │
│        │   Subsequent Requests                 │               │
│        │                                        │               │
│        │   All requests include:                │               │
│        │   Cookie: auth_session=...             │               │
│        │                                        │               │
│        │   Server validates on each request     │               │
│        └──────────────────────────────────────┘               │
│                                                                │
└───────────────────────────────────────────────────────────────┘
```

## Authentication Flow Diagram

```
START
  │
  ▼
┌─────────────────────┐
│  User Visits /login │
  └────────┬──────────┘
           │
           ▼
       ┌───────────────────────┐
       │ Is user authenticated?│
       └───────┬───────────────┘
         Yes ╱ └─ No
           ╱      │
          ╱        ▼
         ╱   ┌──────────────────┐
        ╱    │  Show Login Form  │
       ╱     └────────┬─────────┘
      ╱              │
  ┌──────────┐       ▼
  │ Redirect │  ┌─────────────────────────┐
  │ to       │  │ User Submits Credentials│
  │Dashboard │  │ (email, password)       │
  └──────────┘  └────────┬────────────────┘
      ▲                  │
      │                  ▼
      │          ┌──────────────────────┐
      │          │ loginAction()         │
      │          │ Validate Email/Pass   │
      │          └────────┬─────────────┘
      │                   │
      │        ┌──────────┴──────────┐
      │        │                     │
      │    Valid               Invalid
      │        │                     │
      │        ▼                     ▼
      │  ┌──────────────┐      ┌─────────────┐
      │  │Create Session│      │Show Error   │
      │  │Set Cookie    │      │Message      │
      │  └──────┬───────┘      └────┬────────┘
      │         │                   │
      │         ▼                   ▼
      └─────Redirect to        Stay on
            Dashboard          Login Page


PROTECTED PAGE ACCESS
  │
  ▼
┌──────────────────────┐
│ User Visits /page    │
└──────────┬───────────┘
           │
           ▼
    ┌────────────────────┐
    │getCurrentUser()    │
    │Check Cookie Valid  │
    └────────┬───────────┘
      ┌──────┴──────┐
      │             │
    Valid        Invalid/Expired
      │             │
      ▼             ▼
  ┌────────┐   ┌─────────────┐
  │User OK │   │Redirect to  │
  │        │   │/login       │
  └────┬───┘   └─────────────┘
       │
       ▼
  ┌──────────────────────┐
  │Check User Role       │
  └────────┬─────────────┘
     ┌─────┴────────┐
     │              │
  Authorized    Unauthorized
     │              │
     ▼              ▼
  ┌────────┐   ┌─────────────────┐
  │Render  │   │Redirect to      │
  │Page    │   │/workflows       │
  └────────┘   └─────────────────┘
```

## File Organization

```
Liyali Gateway Project
│
├── src/
│   ├── lib/
│   │   └── auth.ts                  (Core Auth Logic)
│   │       ├── DEMO_USERS map
│   │       ├── getSession()
│   │       ├── getCurrentUser()
│   │       ├── login()
│   │       ├── logout()
│   │       ├── hasRole()
│   │       └── isAdmin()
│   │
│   ├── auth.ts                      (Public API)
│   │   └── Re-exports from lib/auth.ts
│   │
│   ├── app/
│   │   ├── login/
│   │   │   ├── page.tsx            (Login Page)
│   │   │   └── _components/
│   │   │       └── login-form.tsx   (Login Form)
│   │   │
│   │   ├── _actions/
│   │   │   ├── auth.ts             (Old auth actions)
│   │   │   │   ├── getCurrentUser()
│   │   │   │   ├── signOutAction()
│   │   │   │   └── verifyAdminRole()
│   │   │   │
│   │   │   └── auth-actions.ts     (New auth actions)
│   │   │       ├── loginAction()
│   │   │       ├── logoutAction()
│   │   │       ├── requireAuth()
│   │   │       └── requireRole()
│   │   │
│   │   ├── workflows/
│   │   │   ├── dashboard/
│   │   │   │   └── page.tsx        (Protected: Any user)
│   │   │   ├── search/
│   │   │   │   └── page.tsx        (Protected: Any user)
│   │   │   └── requisitions/create/
│   │   │       └── page.tsx        (Protected: Any user)
│   │   │
│   │   ├── admin/
│   │   │   ├── reports/
│   │   │   │   └── page.tsx        (Protected: ADMIN/COMPLIANCE)
│   │   │   ├── users/
│   │   │   │   └── page.tsx        (Protected: ADMIN only)
│   │   │   └── logs/
│   │   │       └── page.tsx        (Protected: ADMIN/COMPLIANCE)
│   │   │
│   │   ├── compliance/
│   │   │   └── tracking/
│   │   │       └── page.tsx        (Protected: ADMIN/COMPLIANCE)
│   │   │
│   │   ├── monitoring/
│   │   │   └── page.tsx            (Protected: ADMIN/COMPLIANCE)
│   │   │
│   │   └── verification/qr/
│   │       └── page.tsx            (Protected: Any user)
│   │
│   └── types/
│       └── workflow.ts             (Contains APIResponse type)
│
├── AUTH_SYSTEM.md                   (Full Documentation)
├── QUICK_START_AUTH.md              (Quick Start Guide)
├── MIGRATION_SUMMARY.md             (Migration Details)
└── AUTH_ARCHITECTURE.md             (This File)
```

## Data Flow for Login

```
┌────────────────────────────────────────────────────────────────┐
│ 1. USER SUBMITS FORM                                            │
│    Email: admin@liyali.com                                      │
│    Password: password123                                         │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. loginAction(email, password) CALLED                          │
│    (Server Action - handles form submission)                    │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. authLogin(email, password) CALLED                            │
│    (Core auth function in src/lib/auth.ts)                      │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. VALIDATE CREDENTIALS                                         │
│    userConfig = DEMO_USERS[email.toLowerCase()]                │
│                                                                 │
│    If not found:                                                │
│      return { success: false, error: 'User not found' }        │
│                                                                 │
│    If found, check password:                                    │
│      if (userConfig.password !== password)                     │
│        return { success: false, error: 'Invalid password' }    │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. CREATE SESSION OBJECT                                        │
│    session = {                                                  │
│      user: {                                                    │
│        id: 'user-007',                                          │
│        name: 'Admin User',                                      │
│        email: 'admin@liyali.com',                              │
│        role: 'ADMIN',                                           │
│        department: 'Administration',                            │
│        avatar: '⚙️'                                             │
│      },                                                         │
│      expiresAt: Date.now() + 86400000  (24 hours from now)    │
│    }                                                            │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 6. ENCODE SESSION TO COOKIE VALUE                               │
│    sessionToken = Base64(JSON.stringify(session))              │
│    Result: "eyJ1c2VyIjp7ImlkIjoiv..."                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 7. SET HTTP-ONLY COOKIE                                         │
│    await cookies().set('auth_session', sessionToken, {         │
│      httpOnly: true,                                            │
│      secure: false,              (true in production)           │
│      sameSite: 'lax',                                           │
│      maxAge: 86400                (24 hours in seconds)         │
│    })                                                           │
│                                                                 │
│    Response Header:                                             │
│    Set-Cookie: auth_session=eyJ1c2Vy...; HttpOnly; Path=/...   │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 8. BROWSER RECEIVES RESPONSE                                    │
│    Stores cookie in browser's cookie jar                        │
│    Cookie automatically sent with all subsequent requests       │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 9. RETURN SUCCESS & REDIRECT                                    │
│    return { success: true, user: userConfig.user }             │
│    (Client-side router.push('/workflows/dashboard'))           │
└────────────────────────────────────────────────────────────────┘
```

## Data Flow for Protected Page Access

```
┌────────────────────────────────────────────────────────────────┐
│ 1. USER NAVIGATES TO PROTECTED PAGE                             │
│    GET /workflows/dashboard                                     │
│    Cookie: auth_session=eyJ1c2Vy...                            │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. PAGE.TSX EXECUTES (Server Component)                         │
│    import { getCurrentUser } from '@/auth'                      │
│    const user = await getCurrentUser()                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. getCurrentUser() CALLED                                      │
│    (From src/lib/auth.ts)                                       │
│    ├─ Get session from cookies                                  │
│    ├─ Decode Base64 → JSON                                     │
│    └─ Return user if valid                                      │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. CHECK SESSION VALIDITY                                       │
│                                                                 │
│    const sessionToken = cookieStore.get('auth_session')?.value │
│                                                                 │
│    If not found or invalid:                                     │
│      return null                                                │
│                                                                 │
│    const session = JSON.parse(Base64.decode(sessionToken))     │
│                                                                 │
│    If session.expiresAt < Date.now():                          │
│      // Session expired                                         │
│      cookies.delete('auth_session')                            │
│      return null                                                │
│                                                                 │
│    return session  // Valid session!                            │
└────────────────────────────────────────────────────────────────┘
                              │
                   ┌──────────┴──────────┐
                   │                     │
              User Found             No User
                   │                     │
                   ▼                     ▼
         ┌──────────────────┐   ┌─────────────────┐
         │Page renders      │   │redirect('/login')
         │with user data    │   │                 │
         └──────────────────┘   └─────────────────┘
                   │
                   ▼
         ┌──────────────────┐
         │CHECK ROLE        │
         │(if needed)       │
         │                  │
         │hasRole check     │
         └────────┬─────────┘
                  │
          ┌───────┴────────┐
          │                │
      Authorized       Unauthorized
          │                │
          ▼                ▼
    ┌──────────┐    ┌─────────────────┐
    │Render    │    │redirect('/workflows')
    │Page      │    │                 │
    └──────────┘    └─────────────────┘
```

## Cookie Lifecycle

```
TIME: 0:00
┌─────────────────────────────────┐
│ User logs in                     │
│ Cookie created                  │
│ Value: Base64(user + timestamp) │
│ Expires: 24:00                  │
└─────────────────────────────────┘

TIME: 12:00
┌─────────────────────────────────┐
│ User still active                │
│ Makes requests                  │
│ Cookie sent with each request   │
│ Still valid (expiresAt future)  │
└─────────────────────────────────┘

TIME: 23:59
┌─────────────────────────────────┐
│ User still active                │
│ Cookie expires in 1 minute      │
│ System still accepts cookie     │
└─────────────────────────────────┘

TIME: 24:00
┌─────────────────────────────────┐
│ Cookie EXPIRED                  │
│ Next request fails validation   │
│ User redirected to /login       │
│ Cookie deleted from browser     │
└─────────────────────────────────┘

TIME: 24:05
┌─────────────────────────────────┐
│ Attempting to access page       │
│ No valid cookie found           │
│ Redirected to /login            │
│ User must log in again          │
└─────────────────────────────────┘
```

## Role-Based Access Control (RBAC) Diagram

```
Authenticated User
        │
        ▼
┌──────────────────────────┐
│ Check User Role          │
│                          │
│ user.role = ?            │
└──────────────┬───────────┘
               │
    ┌──────────┴──────────┬─────────────────┬─────────────┐
    │                     │                 │             │
  REQUESTER        DEPARTMENT_MANAGER   FINANCE_OFFICER  DIRECTOR
    │                     │                 │             │
    ▼                     ▼                 ▼             ▼
  ✓ Workflows          ✓ Workflows      ✓ Workflows    ✓ Workflows
  ✓ Requisitions       ✓ Requisitions   ✓ Requisitions ✓ Requisitions
  ✓ QR Verify          ✓ QR Verify      ✓ QR Verify    ✓ QR Verify
  ✗ Admin              ✗ Admin          ✗ Admin        ✗ Admin
  ✗ Reports            ✗ Reports        ✗ Reports      ✗ Reports
  ✗ Compliance         ✗ Compliance     ✗ Compliance   ✗ Compliance
  ✗ Monitoring         ✗ Monitoring     ✗ Monitoring   ✗ Monitoring
    │                     │                 │             │
    └─────────────────────┴─────────────────┴─────────────┘
                          │
                ┌─────────┴─────────┐
                │                   │
              CFO            COMPLIANCE_OFFICER
                │                   │
                ▼                   ▼
              ✓ Workflows       ✓ Workflows
              ✓ Requisitions    ✓ Requisitions
              ✓ QR Verify       ✓ QR Verify
              ✗ Admin           ✗ Admin (Users)
              ✗ Reports         ✓ Reports
              ✗ Compliance      ✓ Compliance
              ✗ Monitoring      ✓ Monitoring
                │                   │
                └────────┬──────────┘
                         │
                    ┌────▼────┐
                    │          │
                   ADMIN       │
                    │          │
                    ▼          │
                 ✓ All         │
                 ✓ Admin       │
                 ✓ Reports     │
                 ✓ Users       │
                 ✓ Compliance  │
                 ✓ Monitoring  │
                    │          │
                    └────┬─────┘
                         ▼
                   Full Access
```

## Session Validation Algorithm

```
function getCurrentUser():
  1. Get auth_session cookie from request

  2. If cookie doesn't exist:
     └─ return null (not authenticated)

  3. Decode Base64 to JSON:
     sessionToken = Buffer.from(cookie).toString()
     session = JSON.parse(sessionToken)

  4. Check expiration:
     if (session.expiresAt < Date.now()):
       └─ Cookie expired
          └─ Delete cookie
          └─ return null

  5. If all checks pass:
     └─ return session.user
        ├─ id
        ├─ name
        ├─ email
        ├─ role
        ├─ department
        └─ avatar
```

---

This architecture ensures:
- ✅ Stateless authentication (no server-side session store)
- ✅ Secure cookie handling (HttpOnly, SameSite)
- ✅ Automatic session expiration
- ✅ Role-based page access
- ✅ Simple, fast validation on each request
