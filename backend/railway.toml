[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**"]

[deploy]
preDeployCommand = "/app/migrate"
startCommand = "/app/main"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
